apiVersion: v1
kind: Service
metadata:
  name: iperf-server
spec:
  type: NodePort
  selector:
    app: iperf-server
  ports:
  - name: tcp-server
    protocol: TCP
    port: 12865
    targetPort: server

